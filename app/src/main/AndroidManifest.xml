<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:name=".Util.ApplicationUtil"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.DateCourseRecommendApplication">



        <!-- 로그인 액티비티: 앱 시작점 -->
        <activity
            android:name=".Activity.UtilForUI.SplashActivity"
            android:exported="true"
            android:theme="@style/Theme.DateCourseRecommendApplication">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>
        <activity android:name=".Activity.LoginActivity"/>
        <activity android:name=".Activity.ForPost.OpenPostActivity"/>
        <activity android:name=".Activity.User.UserSetUpActivity"/>
        <activity android:name=".Activity.ForPost.RetweetActivity"/>
        <activity android:name=".Activity.ForPost.WritePostActivity"/>
        <activity android:name=".Activity.User.UserCreateActivity"/>
        <activity android:name=".Activity.User.UserProfileEditActivity"/>
        <activity android:name=".Activity.ForPost.EditPostActivity"/>
        <activity android:name=".Activity.UtilForUI.SearchLocationActivity"/>
        <activity android:name=".Activity.UtilForUI.ViewMyPostsActivity"/>
        <activity android:name=".Activity.UtilForUI.ViewMyLikesActivity"/>

        <activity
            android:name=".Activity.MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.DateCourseRecommendApplication">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>